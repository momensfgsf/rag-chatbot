# RAG Chatbot (PDF Text Extractor)

This project is the beginning of a RAG chatbot.
Currently it extracts text from a PDF and prints a preview.

## Setup
```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
